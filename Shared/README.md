# Shared

Cross-cutting UI and utilities (DesignSystem, Components, Extensions,
Utilities). Populated on demand from Milestone 2 — empty by design until
there is real shared code. Never business logic, never Core logic.
