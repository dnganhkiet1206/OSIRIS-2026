# Resources

Assets, localization and sample data — never mixed with source. The asset
catalog is created with the Xcode project (Milestone 2). Config/ is separate:
it holds behavior configuration, not assets.
